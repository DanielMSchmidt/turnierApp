!!!
%html
  %head
    %title TurnierApp | Organisiere deine Turniere
    %meta{:content => "Turnier App hilft Turniertänzern und Vereinen dabei, den Überblick üeber bereits getanzte Turniere und die Erfolge über die Zeit hinweg zu behalten", :name => "description"}
    %meta{:content => "Tanzen, Turniertanzen, Sport, Turniersport, Latein, Standard, Turniere, organisieren", :name => "keywords"}
    %link{:href => "/favicon.ico", :rel => "shortcut icon"}
    %link{:href => "http://cdn.oesmith.co.uk/morris-0.4.1.min.css", :rel => "stylesheet"}
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body
    = hidden_field('Navigation', 'active', value: @active_page || 'Start')
    :javascript
      $("li").removeClass("active");
      $('.#{@active_page || 'Start'}').addClass('active');
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          = link_to 'TurnierApp', root_path, class: 'brand'
          .nav-collapse.collapse.navbar-responsive-collapse
            %ul.nav
              - if user_signed_in?
                %li.clubs
                  = link_to 'Verein hinzufügen', '#add_club_modal', role: 'button', data: {toggle: 'modal'}
                %li
                  = link_to 'Ausstehende Anmeldungen', '#pending_club_modal', role: 'button', data: {toggle: 'modal'}
                %li.profil
                  = link_to "Profil bearbeiten", edit_user_registration_path
                %li.signout
                  = link_to "Abmelden", destroy_user_session_path, method: :delete
              - else
                %li.signup
                  = link_to "Registrieren", new_user_registration_path
                %li.signin
                  = link_to "Anmelden", new_user_session_path
              %li.impressum
                = link_to 'Impressum', impressum_path

    .content
      .row-fluid
        .offset1.span10
          = yield

    .assets
      - if user_signed_in?
        = render "shared/user_modals"
        = render "shared/modals/add_tournament_modal"

      - if Rails.env.production?
        :javascript
          var _gaq = _gaq || [];
          _gaq.push(['_setAccount', 'UA-37020620-1']);
          _gaq.push(['_setDomainName', 'herokuapp.com']);
          _gaq.push(['_trackPageview']);

          (function() {
            var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
            var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
          })();
