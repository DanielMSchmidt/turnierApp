<p id="notice"><%= notice %></p>

<table class="tournament-list">
  <tr>
    <th>Paar</th>
    <th>Datum</th>
    <th>Zeit</th>
    <th>Art</th>
    <th>Platz</th>
    <th>Adresse</th>
    <th>Turniernr.</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <%= render @tournament %>
</table>

<% if current_user == @user %>
  <%= link_to 'Bearbeiten', edit_tournament_path(@tournament), class: "btn btn-warning" %> |
<% end %>
<%= link_to 'Zurück', tournaments_path %>
