= form_for(current_user.activeCouple.standard, remote: true) do |f|
  .help-block
    Die Punkte und Platzierungen, die du hier angibst werden auf deine bisherigen Turnierergebnisse addiert.

  .row-fluid
    .span6
      .input-prepend
        .add-on
          Punkte
        = f.number_field :start_points, :class => "span6"

      .input-prepend
        .add-on
          Platzierungen
        = f.number_field :start_placings, :class => "span4"

      .actions
        = f.submit 'Speichern', :class => "btn btn-primary", remote: true

    .span4
      .btn-group
        = link_to 'Aufgestiegen!', couple_level_up_path(kind: 'standard'), :class => "btn btn-primary", data: {dismiss: 'modal'}, remote: true
        = link_to 'Nochmal!', couple_reset_path(kind: 'standard'), :class => "btn btn-primary", data: {dismiss: 'modal'}, remote: true