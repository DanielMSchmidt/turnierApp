%p#notice= notice
%h1
  Turniere des #{@club.name}
%br/
%h3 Statistiken
= raw(print_stats @users.collect{|x| x.tournaments}.flatten)
%h3 Mitglieder
%br/
%ul
  - @users.each do |user|
    %li= link_to "#{user.name}", user_tournaments_path(user)
- if current_user.id == @club.user_id
  = render 'owner_area'
= link_to 'Zurueck', clubs_path
