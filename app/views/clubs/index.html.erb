<h1>Alle Vereine</h1>

<table class="table table-striped">
<% @clubs.each do |club| %>
  <tr>
    <td><%= link_to club.name, club %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Verein hinzufügen', new_club_path %>
