%h1 Listing progresses

%table
  %tr
    %th Couple
    %th Kind
    %th Start points
    %th Start placings
    %th Start class
    %th Finished
    %th
    %th
    %th

  - @progresses.each do |progress|
    %tr
      %td= progress.couple_id
      %td= progress.kind
      %td= progress.start_points
      %td= progress.start_placings
      %td= progress.start_class
      %td= progress.finished
      %td= link_to 'Show', progress
      %td= link_to 'Edit', edit_progress_path(progress)
      %td= link_to 'Destroy', progress, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Progress', new_progress_path
